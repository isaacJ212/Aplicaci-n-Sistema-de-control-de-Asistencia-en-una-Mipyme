version: '3.8'

services:
  api:
    build:
      context: .
      dockerfile: ./backend/dockerfile
    container_name: mipyme-asistencia-api
    ports:
      - "8080:8080"
    environment:
      ASPNETCORE_ENVIRONMENT: Development
      ASPNETCORE_URLS: http://+:8080
    restart: unless-stopped
